apiVersion: apps/v1
kind: Deployment
metadata:
  name: delete-service
  labels:
    app.kubernetes.io/name: delete-service
    app.kubernetes.io/part-of: overkilled-todo
    app.kubernetes.io/component: crud-service
    app.kubernetes.io/managed-by: kustomize
spec:
  replicas: 2
  selector:
    matchLabels:
      app: delete-service
  template:
    metadata:
      labels:
        app: delete-service
        app.kubernetes.io/name: delete-service
        app.kubernetes.io/part-of: overkilled-todo
        app.kubernetes.io/component: crud-service
        app.kubernetes.io/managed-by: kustomize
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: delete-service
          image: delete-service:latest
          ports:
            - containerPort: 5004
          readinessProbe:
            tcpSocket:
              port: 5004
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 5004
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          env:
            - name: DATABASE_HOST
              value: mariadb
            - name: DATABASE_USER
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: MYSQL_USER
            - name: DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: MYSQL_PASSWORD
            - name: DATABASE_NAME
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: MYSQL_DATABASE
            - name: DATABASE_PORT
              value: "3306"
---
apiVersion: v1
kind: Service
metadata:
  name: delete-service
  labels:
    app.kubernetes.io/name: delete-service
    app.kubernetes.io/part-of: overkilled-todo
    app.kubernetes.io/component: crud-service
    app.kubernetes.io/managed-by: kustomize
spec:
  selector:
    app: delete-service
  ports:
    - protocol: TCP
      port: 5004
      targetPort: 5004
